# Extract aligned sample metadata

Extract aligned sample metadata

## Usage

``` r
mae_samples(x, experiment)
```

## Arguments

- x:

  A `MultiAssayExperiment` object.

- experiment:

  Optional experiment name.

## Value

A base `data.frame`, with row names matching assay columns.
