% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clinical.R
\name{surv_auc_table}
\alias{surv_auc_table}
\title{Tabulate time-dependent AUC}
\usage{
surv_auc_table(x, experiment, time, event, marker, times, cause = 1, ...)
}
\arguments{
\item{x}{A \code{timeROC} result, a data frame with \code{time} and \code{auc}, or a
\code{MultiAssayExperiment} used to estimate AUC(t).}

\item{experiment}{Optional experiment name.}

\item{time, event}{Metadata columns containing follow-up time and event.}

\item{marker}{Metadata column or numeric marker vector.}

\item{times}{Evaluation times.}

\item{cause}{Event code treated as the outcome of interest.}

\item{...}{Additional arguments passed to \code{\link[timeROC:timeROC]{timeROC::timeROC()}}.}
}
\value{
A data frame with \code{time}, \code{auc}, \code{n_case}, \code{n_control}, and
\code{estimator}.
}
\description{
Builds a plot-ready AUC(t) table from an already-computed \code{timeROC} result
or from aligned follow-up and a marker. When \code{x} is a MultiAssayExperiment
and \code{timeROC} is installed, values come from \code{\link[=surv_roc]{surv_roc()}}. Otherwise the
helper computes a simple cumulative/dynamic AUC: cases are events of
interest by time \emph{t}, and controls remain at risk after \emph{t}. That fallback
is diagnostic and is not Uno's IPCW estimator.
}
