% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clinical.R
\name{drug_lincs_table}
\alias{drug_lincs_table}
\title{Extract a ranked LINCS connectivity table}
\usage{
drug_lincs_table(result, score = NULL)
}
\arguments{
\item{result}{A \code{gessResult} or a data frame with LINCS/CMap-style columns.}

\item{score}{Optional score column to rank by. When \code{NULL}, prefer a finite
\code{NCS} column, then \code{Tau}, \code{WTCS}, or \code{NCSct}.}
}
\value{
A data frame ranked by the resolved score, with \code{score_column},
\code{score_label}, and \code{direction} attached. \code{direction} is the sign of the
score (\code{Reverse}, \code{Mimic}, or \code{Unrelated}), not a wet-lab result.
}
\description{
Converts a native signatureSearch \code{gessResult} from \code{\link[=drug_lincs]{drug_lincs()}}, or a
data frame with the same columns, into a ranked table. The helper does not
recompute weighted connectivity scores. The score column used for ranking
is the one \code{\link[=drug_lincs]{drug_lincs()}} actually stored (\code{NCS} by default; \code{Tau} only
when it was requested and is finite).
}
