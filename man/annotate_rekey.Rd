% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resources.R
\name{annotate_rekey}
\alias{annotate_rekey}
\title{Apply an identifier mapping to common gene-level values}
\usage{
annotate_rekey(
  values,
  mapping,
  source = "source_id",
  target = "target_id",
  duplicates = c("sum", "mean", "max_abs", "first"),
  drop_unmapped = TRUE
)
}
\arguments{
\item{values}{A numeric gene-by-sample matrix, a named numeric vector, or a
character vector of identifiers.}

\item{mapping}{A data frame containing source and target identifier columns.}

\item{source, target}{Column names in \code{mapping}.}

\item{duplicates}{Aggregation rule for numeric values sharing a target.}

\item{drop_unmapped}{Drop source identifiers without a non-missing target.
When \code{FALSE}, their original source identifiers are retained.}
}
\value{
A target-keyed numeric matrix, named numeric vector, or character
vector, matching the input value type.
}
\description{
A source identifier that maps to multiple targets is expanded to every
target. When multiple input rows or elements then share a target, the
explicitly selected \code{duplicates} rule is applied. Exact duplicate mapping
rows are removed before expansion so they cannot multiply a value.
}
