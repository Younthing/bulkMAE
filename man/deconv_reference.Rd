% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deconvolution.R
\name{deconv_reference}
\alias{deconv_reference}
\title{Construct a single-cell deconvolution reference}
\usage{
deconv_reference(
  counts,
  cell_data,
  cell_type,
  sample,
  cell_state = NULL,
  assay_name = "counts"
)
}
\arguments{
\item{counts}{Raw integer count matrix with genes in rows and cells in
columns. Dense matrices and sparse \code{Matrix} objects are accepted without
densifying the latter. Complete, unique gene and cell names are required.}

\item{cell_data}{Cell-level metadata with row names containing every count
matrix cell name exactly once. Rows are aligned by cell name.}

\item{cell_type, sample}{A column name in \code{cell_data}, or a named vector with
one non-missing label per cell. Values are stored in the canonical
\code{cell_type} and \code{sample_id} columns.}

\item{cell_state}{Optional cell-state column name or named vector. When
supplied, values are stored in the canonical \code{cell_state} column.}

\item{assay_name}{Name used for the raw-count assay.}
}
\value{
A \code{SingleCellExperiment} with aligned cell metadata.
}
\description{
Builds the canonical \code{SingleCellExperiment} reference accepted directly by
\code{\link[=deconv_music]{deconv_music()}} and \code{\link[=deconv_bayesprism]{deconv_bayesprism()}}. This helper only standardizes a
user-supplied single-cell count matrix and its metadata; it does not provide
or simulate a biological reference.
}
