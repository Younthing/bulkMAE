% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deconvolution.R
\name{deconv_fractions}
\alias{deconv_fractions}
\title{Extract a cell-type by sample fraction matrix}
\usage{
deconv_fractions(
  result,
  orientation = c("cells_by_samples", "samples_by_cells")
)
}
\arguments{
\item{result}{A deconvolution table, MuSiC list, or numeric matrix.}

\item{orientation}{Layout used when \code{result} is a bare matrix or data
frame without a \code{cell_type} column.}
}
\value{
A cell-type-by-sample numeric matrix. This is not a new result
class.
}
\description{
Accepts the native immunedeconv table (\code{cell_type} plus sample columns),
a MuSiC result list containing \code{Est.prop.weighted}, or an already named
numeric matrix. Matrix layout defaults to cell types in rows (the same
orientation as a bulkMAE assay). MuSiC's sample-by-cell estimates are
transposed automatically. BayesPrism users should pass the matrix from
\code{BayesPrism::get.fraction()} with
\code{orientation = "samples_by_cells"}.
}
