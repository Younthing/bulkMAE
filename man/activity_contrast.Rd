% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/result-inputs.R
\name{activity_contrast}
\alias{activity_contrast}
\title{Compare already-inferred sample activities between two groups}
\usage{
activity_contrast(
  result,
  sample_data = NULL,
  group,
  reference = NULL,
  target = NULL,
  test = c("welch", "wilcox", "none"),
  value = "score",
  source = "source",
  sample = "condition",
  statistic = NULL,
  statistic_column = "statistic"
)
}
\arguments{
\item{result}{A data-frame-like activity result.}

\item{sample_data}{Optional sample metadata whose row names cover every
activity sample. Required when \code{group} is a column name.}

\item{group}{A metadata column in \code{sample_data} or a complete sample-named
vector of group labels.}

\item{reference, target}{Group levels used to form \code{target - reference}.
When both are \code{NULL} and \code{group} has exactly two levels, the first
remaining level is the reference.}

\item{test}{\code{"welch"} for a two-sample Welch \emph{t}-test, \code{"wilcox"} for a
Wilcoxon rank-sum test, or \code{"none"} to return mean differences only.}

\item{value, source, sample}{Column names containing activity values, sources,
and sample identifiers.}

\item{statistic}{Optional statistic/method value to retain when a result
contains multiple methods.}

\item{statistic_column}{Column containing statistic/method names.}
}
\value{
A data frame with one row per regulator/source:
\code{source}, \code{reference}, \code{target}, \code{n_reference}, \code{n_target},
\code{mean_reference}, \code{mean_target}, \code{delta}, \code{statistic}, \code{p_value}, and
\code{adjusted_p_value}.
}
\description{
This helper does not run decoupleR again and does not perform gene-set
enrichment. It takes the long-format table returned by
\code{\link[=activity_decouple]{activity_decouple()}}, \code{\link[=activity_progeny]{activity_progeny()}}, or \code{\link[=activity_tf]{activity_tf()}} and
computes a two-group contrast of those per-sample activity scores. The
\code{delta} column is the difference of group means (\code{target - reference}).
Optional Welch or Wilcoxon \emph{p}-values describe that comparison; they are
not enrichment \emph{p}-values and they are not the per-sample \code{p_value}
column returned by decoupleR.
}
\examples{
result <- expand.grid(
  source = c("TF_A", "TF_B"),
  condition = paste0("s", 1:6),
  stringsAsFactors = FALSE
)
result$statistic <- "ulm"
result$score <- c(rep(c(-1, 1), each = 3), rep(c(2, -2), each = 3))
samples <- data.frame(
  dex = factor(rep(c("untrt", "trt"), each = 3)),
  row.names = paste0("s", 1:6)
)
activity_contrast(
  result, samples, group = "dex",
  reference = "untrt", target = "trt"
)
}
