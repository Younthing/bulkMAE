% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clinical.R
\name{surv_cox_univariable}
\alias{surv_cox_univariable}
\title{Fit univariable Cox models for selected predictors}
\usage{
surv_cox_univariable(
  x,
  experiment,
  time,
  event,
  predictors,
  assay = NULL,
  features = NULL,
  conf_level = 0.95,
  ...
)
}
\arguments{
\item{x}{A \code{MultiAssayExperiment} object.}

\item{experiment}{Optional experiment name.}

\item{time, event}{Metadata columns containing follow-up time and event.}

\item{predictors}{Unique metadata column names and/or expression feature
identifiers. Feature names must also appear in \code{features}.}

\item{assay}{Assay name or one-based assay index.}

\item{features}{Optional expression features appended to the model data.}

\item{conf_level}{Confidence level for the hazard-ratio interval.}

\item{...}{Additional arguments passed to \code{\link[survival:coxph]{survival::coxph()}}.}
}
\value{
A data frame in the \code{\link[=surv_cox_table]{surv_cox_table()}} schema, with one or more
rows per predictor.
}
\description{
Fits one \code{\link[=surv_cox]{surv_cox()}} model per predictor and stacks the coefficient tables.
Clinical predictors are read from aligned sample metadata. Expression
features are appended only when they are named in \code{features} and \code{assay} is
supplied. This is a convenience loop around the existing Cox wrapper, not a
new estimator.
}
