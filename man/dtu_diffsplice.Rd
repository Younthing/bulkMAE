% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/differential.R
\name{dtu_diffsplice}
\alias{dtu_diffsplice}
\title{Test differential exon or transcript usage from a fitted limma/edgeR model}
\usage{
dtu_diffsplice(
  fit,
  gene_id,
  feature_id = NULL,
  robust = FALSE,
  coef = NULL,
  contrast = NULL,
  ...
)
}
\arguments{
\item{fit}{A native \code{MArrayLM} or \code{DGEGLM} model fit.}

\item{gene_id}{Gene identifier for every fitted feature.}

\item{feature_id}{Optional exon or transcript identifier.}

\item{robust}{Use robust empirical Bayes estimation.}

\item{coef, contrast}{Coefficient or contrast for the edgeR method. Supply at
most one. For limma, apply contrasts to \code{fit} before calling this function;
limma's \code{diffSplice()} calculates statistics for the fitted coefficients.}

\item{...}{Additional arguments passed to the selected backend's
\code{diffSplice()} method.}
}
\value{
A native limma or edgeR differential-splicing result.
}
\description{
Test differential exon or transcript usage from a fitted limma/edgeR model
}
\details{
\code{gene_id} and \code{feature_id} are aligned to the fitted rows when
they are named. This is particularly useful after expression filtering.
Supply an untested \code{DGEGLM} to the edgeR method; choose its test with \code{coef}
or \code{contrast}. For limma,
apply the desired contrast with \code{\link[limma:contrasts.fit]{limma::contrasts.fit()}} before calling this
function when a contrast, rather than a coefficient, is required.
}
